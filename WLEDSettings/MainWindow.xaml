<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WLEDSettings.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WLEDSettings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winui="using:Microsoft.UI.Xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    Title="WLED Settings">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <StackPanel>

        <Grid x:Name="AppTitleBar"  
          Height="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
                <ColumnDefinition/>
                <ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TitleBarTextBlock" 
                   Text="WLED On/Off Automation" 
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Grid.Column="1"
                   VerticalAlignment="Center"
                   Margin="10,0,0,0"/>
        </Grid>

        <Grid Padding="16,0" RowDefinitions="Auto,Auto,*,Auto">
            <StackPanel>
                <TextBlock Text="Configure your WLED device" />
            </StackPanel>

            <StackPanel Grid.Row="1" Spacing="10" Margin="0,16,0,0">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="7*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="HostBox" Header="Hostname / IP" PlaceholderText="192.168.1.191" Grid.Column="0"/>

                    <TextBox x:Name="PortBox" Header="Port" Text="80" InputScope="Number" Grid.Column="1"/>
                </Grid>
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Ellipse x:Name="StatusDot" Width="10" Height="10" Fill="Orange"/>
                    <TextBlock x:Name="StatusText" Text="Connecting..." VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock x:Name="InfoText" Text="-" Opacity="0.7"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Spacing="8" Margin="0,16,0,0">
                <TextBlock Text="Color" FontWeight="SemiBold"/>
                <controls:ColorPicker
                    x:Name="Picker"
                    IsMoreButtonVisible="False"
                    IsColorSliderVisible="True"
                    IsColorChannelTextInputVisible="False"
                    IsHexInputVisible="False"
                    IsAlphaEnabled="False"
                    IsAlphaSliderVisible="False"
                    IsAlphaTextInputVisible="False"
                    />
                <!--ColorSpectrumShape="Ring"-->

            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button x:Name="RetryBtn" Content="Retry Connection" Click="OnRetryClick"/>
                <Button x:Name="SaveBtn" Content="Save" Click="OnSaveClick"/>
            </StackPanel>
        </Grid>

    </StackPanel>


</Window>
