<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WLEDSettings.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WLEDSettings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winui="using:Microsoft.UI.Xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    Title="WLED Settings">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <StackPanel>

        <Grid x:Name="AppTitleBar"  
          Height="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
                <ColumnDefinition/>
                <ColumnDefinition x:Name="RightPaddingColumn" Width="0"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TitleBarTextBlock" 
                   Text="WLED On/Off Automation" 
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Grid.Column="1"
                   VerticalAlignment="Center"
                   Margin="10,0,0,0"/>
        </Grid>

        <Grid Padding="16,0" RowDefinitions="Auto,Auto,Auto,Auto,Auto">
            <StackPanel>
                <TextBlock Text="Configure your WLED device" FontWeight="SemiBold"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Spacing="8" Margin="0,16,0,0">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="7*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="HostBox" Header="Hostname / IP" PlaceholderText="192.168.1.191" Grid.Column="0"/>
                    <TextBox x:Name="PortBox" Header="Port" Text="80" InputScope="Number" Grid.Column="1"/>
                
                </Grid>

                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Ellipse x:Name="StatusDot" Width="10" Height="10" Fill="Orange"/>
                    <TextBlock x:Name="StatusText" Text="Connecting..." />
                    <TextBlock x:Name="InfoText" Text="" Opacity="0.7" />
                </StackPanel>
                
            </StackPanel>

            <StackPanel Grid.Row="2" Spacing="8" Margin="0,16,0,0">
                <TextBlock Text="Color" FontWeight="SemiBold"/>

                <SplitButton x:Name="myColorButton" Margin="0" Padding="0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <Border x:Name="ColorPreview" HorizontalAlignment="Stretch" Height="32" Margin="0" Padding="0" Background="White" CornerRadius="4,0,0,4"/>
                    <SplitButton.Flyout>
                        <Flyout Placement="Auto">

                            <Flyout.FlyoutPresenterStyle>
                                <Style TargetType="FlyoutPresenter">
                                    <Setter Property="Padding" Value="8"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Flyout.FlyoutPresenterStyle>

                            <local:ColorSpectrum 
                            x:Name="MyColorSpectrum"
                            ColorChanged="OnColorSpectrum_ColorChanged" />
                        </Flyout>
                    </SplitButton.Flyout>
                </SplitButton>
           


            </StackPanel>

            <StackPanel Grid.Row="3" Spacing="8" Margin="0,16,0,0">
                <TextBlock Text="Brightness" FontWeight="SemiBold"/>

                <controls:Slider x:Name="BrightnessSlider"
                                 Minimum="0"
                                 Maximum="255"
                                 Value="255"
                                 HorizontalAlignment="Stretch"
                                 ValueChanged="OnBrightnessSliderChanged"/>

            </StackPanel>

            <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button
                    x:Name="SaveBtn"
                    Content="Save configuration"
                    Click="OnSaveClick"
                    Style="{StaticResource AccentButtonStyle}"
                    />
            </StackPanel>
        </Grid>

    </StackPanel>


</Window>
